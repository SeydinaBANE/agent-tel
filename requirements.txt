# Core
fastapi>=0.116.0
uvicorn[standard]>=0.30.0
python-dotenv>=1.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Agno - framework agentique
agno>=1.0.0

# HTTP
httpx>=0.27.0

# Twilio
twilio>=9.0.0

# STT - Whisper local (pas de clé API)
openai-whisper>=20240930

# TTS - edge-tts (Microsoft, gratuit)
edge-tts>=6.1.0

# Compatibilité Python 3.13+ — audioop déprécié
# audioop-lts>=0.2.0  # pip install for Python 3.13+; stt.py has conditional fallback

# WebSocket
websockets>=12.0

# ElevenLabs TTS (optionnel)
# elevenlabs>=1.0.0  # ou via httpx direct (déjà inclus)

# Rate limiting
slowapi>=0.1.9

# Monitoring (optionnel)
sentry-sdk[fastapi]>=2.0.0

# DB async
sqlalchemy[asyncio]>=2.0.50
aiosqlite>=0.20.0
asyncpg>=0.29.0
alembic>=1.13.0

# Google Calendar (optionnel)
google-auth>=2.29.0
google-auth-oauthlib>=1.2.0
google-api-python-client>=2.130.0

# openai SDK (requis par Agno OpenAILike)
openai>=1.30.0
